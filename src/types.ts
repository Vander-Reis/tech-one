export interface ServiceProps {
  icon: React.ReactNode;
  title: string;
  description: string;
}

export interface TestimonialProps {
  name: string;
  image: string;
  testimonial: string;
  position?: string;
  company?: string;
}